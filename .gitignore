# Secrets
opencode.json
.env
.env.local

# Presentation (local only, not pushed)
presentation

# Node
web/node_modules
web/.next
web/out
web/dist
web/build
dist
*.tsbuildinfo

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode
.idea

# ZCP
.zcp

# Docs are large but tracked separately; exclude from deploy artifact
# (we keep them in the repo for the submission, but not deployed)
docs
